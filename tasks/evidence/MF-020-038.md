# MF-020–038 completion evidence

Environment: Windows Azure Dev Box, Python 3.12.1, PyTorch 2.13.0+cpu, no CUDA device. Date:
2026-08-17.

## Delivered

- Frozen 16,384-token byte-level BPE contract with immutable IDs for PAD/BOS/EOS, chat, FIM,
  and tool placeholders; deterministic training/loading CLIs and Unicode/atomic-token tests.
- Provenance-validated documents, permissive code-license enforcement, bounded/resumable/
  deterministically shuffled FineWeb-Edu streaming, hash deduplication/splitting, exclusion
  hashes, EOS packing, remainders, and deterministic worker sharding.
- Preallocated KV cache with transactional appends; full-vs-cached parity for token and chunked
  decode; RoPE offsets; greedy/temperature/top-k/top-p/per-row EOS generation.
- Safetensors weights, trust-scoped local optimizer/RNG state, exact deterministic resume,
  standalone release folders, and sample/chat/export CLIs.
- Weighted validation CE/perplexity/BPB, lm-eval 0.4.12 adapter and suite configuration,
  versioned original code/FIM fixtures, contamination checks, and comparable benchmark records.

## Verification

Focused phase suites passed during implementation:

- tokenizer/data: 16 tests, followed by expanded adversarial data coverage (9 data tests);
- cache/generation/checkpoint: 11 tests;
- evaluation: 5 tests;
- intermediate fast suite: **74 passed, 2 slow tests deselected** in 77.72 seconds;
- final `init.cmd cpu --all-groups` clean bootstrap gate: **86 passed** in 41.02 seconds;
- final post-change full pytest suite: **86 passed** in 39.84 seconds;
- Ruff lint passed and formatting is clean.

The strict M1 CLI gate was rerun at 700 steps and reached **0.0004237432** nats/token.

## 50M CPU integration smoke

Command:

```text
.venv\Scripts\python scripts\smoke_50m.py
```

The one-step FP32 smoke used 327 real project-prose documents and 1,123 available packed
sequences. Its bounded gate consumed one 32-token training sequence and proved:

- exact frozen construction: **53,361,152 parameters**;
- finite loss/backward and optimizer step;
- checkpoint logits were bit-exact after reload;
- cached generation produced two tokens;
- validation metrics and a schema-checked comparison record were emitted;
- measured five-iteration forward rate was **563.87 tokens/s** at context 32 on this CPU;
- the batch-1, capacity-34 KV cache allocated **1,949,696 bytes**.

Committed reports:

- [`reports/50m-edu-cpu-smoke.md`](../../reports/50m-edu-cpu-smoke.md)
- [`reports/50m-edu-cpu-smoke.json`](../../reports/50m-edu-cpu-smoke.json)
- [`reports/runs/50m-edu-cpu-smoke.json`](../../reports/runs/50m-edu-cpu-smoke.json)

This is an engineering integration baseline, not a useful-language-model quality claim. The
1–5M-token FineWeb-Edu run, CUDA dtype parity, peak VRAM, and standard ARC-Easy/HellaSwag/PIQA
scores remain explicitly deferred to the home RTX environment under MF-063.

## 2026-08-18 review amendments

- MF-002 remains correctly closed: `.github/workflows/ci.yml` exists and runs the Windows CPU
  sync, Ruff lint, and non-slow pytest suite. The public-release workflow check remains MF-068.
- FineWeb-Edu no longer records `sample-10BT` as though it were a source revision. The adapter
  passes and records immutable Hub revision
  `87f09149ef4734204d70ed1d046ddc9ca3f2b8f9`; the network-free adapter test asserts the exact
  dataset/config/revision request.
- Edu full-vs-token and full-vs-chunk cache tests now require exact per-position argmax agreement
  in addition to fixed FP32 numerical tolerance.
- Generic M2 filtering is documented as structural smoke filtering only. Real web/code admission,
  bounded-memory shards, Windows-safe workers, mixed-precision cache dtype, CPU token-ID checks,
  and GPU-only performance/effect evidence have explicit owners in MF-045–051 and MF-063.

# MF-001–019 completion evidence

Environment: Windows Azure Dev Box, Python 3.12.1, PyTorch 2.13.0+cpu, no CUDA device. Date: 2026-08-17.

## Bootstrap and portability

- `init.cmd cpu` completed from the repository root.
- A second `init.cmd cpu` preserved all source/config files, synchronized the locked CPU extra, imported the package, and ran the suite.
- `uv.lock` resolves mutually exclusive `cpu`, `cu126`, `cu130`, and `cu132` PyTorch 2.13 extras. The RTX machine can use `init.cmd cu130` without editing project metadata.
- Optional `eval` and `labs` groups are locked but installed only with `--all-groups`.

## Quality commands

```text
.venv\Scripts\ruff.exe check .
.venv\Scripts\ruff.exe format --check .
cmd /d /c "init.cmd cpu"
```

The final end-to-end bootstrap collected 41 tests and reported `41 passed in 10.54s`.

Coverage includes validated presets, RNG/run metadata, RMSNorm and SwiGLU output/gradient references, causal/local masks, shifted and assistant-ready masked loss, manual/SDPA output and gradient parity, full-model causality/backward, tied embeddings, uncached generation, and split-half RoPE parity against `transformers.models.llama.apply_rotary_pos_emb`.

## Milestone checks

- Frozen `50m-edu.toml` model instantiation: **53,361,152 trainable parameters**.
- CPU 100-example strict overfit command:

```text
.venv\Scripts\python.exe -m minifrontier.overfit --device cpu --examples 100 --sequence-length 8 --steps 700 --learning-rate 0.005 --output artifacts\overfit\run.json
```

- Loss: **4.6261878 → 0.0004237432 nats/token**, below the strengthened `<1e-3` gate.
- Prompt/continuation token IDs: **[1, 2] → [3, 4, 5, 6]**.
- Machine-readable evidence: `artifacts/overfit/run.json` (intentionally git-ignored).

The feedback hardening also made full-causal SDPA mask-free/fused-eligible, shared explicit
masks across layers, scaled initialization with width/depth, pinned manual/SDPA parity to FP32,
and removed `.item()` synchronizations from forward/loss/RoPE hot paths.

No CUDA benchmark is claimed. The home RTX environment remains the gate for GPU performance and
longer training evidence.

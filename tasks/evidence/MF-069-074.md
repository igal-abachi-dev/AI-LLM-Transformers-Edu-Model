# MF-069–074 implementation evidence

Environment: Windows 11 Azure Dev Box, Python 3.12, PyTorch 2.13 CPU, Transformers 5.15, no CUDA
device, vLLM runtime, llama.cpp checkout, canonical 150M checkpoints, or trained release weights.
Date: 2026-08-18.

## MF-069 — completed software preflight

- Full 350M/500M presets construct on PyTorch's meta device without scale-sized allocations.
- Exact counts are 332,460,544 (`350m-modern`) and 433,914,112 (`500m-modern`).
- Reports label BF16 parameter/gradient, FP32 AdamW-state, inference, and mixed local/global KV byte
  counts as analytic lower bounds. Activations, workspaces, CUDA context, and fragmentation are
  explicitly excluded.
- A tiny Modern projection completes forward/backward and exact safe-weight plus trusted-local
  optimizer/data-cursor checkpoint restore.
- [`reports/m10-scale-preflight-cpu.json`](../../reports/m10-scale-preflight-cpu.json) records every
  CUDA field and the scale decision as `unmeasured`.

## MF-070 — runner complete, measurement open

- Trainer and inference profiles now retain allocated/reserved/total VRAM and reserved fraction.
- The assembler refuses CPU/incomplete records and retains measured validation behavior,
  checkpoint-resume state, failures, throughput, elapsed/projected time, and projected cost.
- The decision function refuses failed or unmeasured evidence and applies predeclared stop criteria.
- No 350M CUDA run or 500M go/no-go decision exists; MF-070 remains In progress.

## MF-071 — local adapter proof complete, canonical/Hub gate open

- A standalone `MiniFrontierConfig`, base model, and causal-LM wrapper preserve MHA/GQA, QK-Norm,
  split-half RoPE, Local/Local/Local/Global, global NoPE, RMSNorm, SwiGLU, and tied embeddings.
- Safe exports contain remote-code `auto_map`, tokenizer/special/FIM/chat metadata, generation
  config, model card, source revision, adapter metadata, and a complete SHA-256 manifest.
- Local `AutoConfig`, `AutoModel`, `AutoModelForCausalLM`, and `AutoTokenizer` load tests pass.
- Native versus Transformers FP32 logits and exact argmax pass for tiny Edu, Modern, global-NoPE,
  and cached chunk fixtures.
- Canonical 150M exports and pinned-Hub revision tests await MF-067, so MF-071 remains In progress.

## MF-072 — compatibility contract complete, CUDA validation open

- The base model forwards kwargs, attention calls Transformers `ALL_ATTENTION_FUNCTIONS`, and the
  model advertises `_supports_attention_backend`. Configuration declares interleaved
  `sliding_attention`/`full_attention` layer types for vLLM.
- WSL2 launch instructions, a pinned-model serve command, native serving fixture generation, and
  completions/chat OpenAI API validation are implemented.
- The evidence schema cannot mark a run passed with missing parity, metrics, artifact hashes, or
  any `unmeasured` field.
- No WSL2/vLLM CUDA load, BF16/cache parity, VRAM/TTFT/decode profile, or coding-client transport run
  exists. Tool calling and native Win32 vLLM are not claimed. MF-072 remains In progress.

## MF-073/074 — fail-closed orchestration complete, external implementation open

- High-precision conversion refuses a non-MiniFrontier source, an unpinned llama.cpp checkout, or a
  checkout lacking MiniFrontier converter, GGUF constants/tensor mapping, architecture
  registration/declaration, and graph markers.
- The current upstream layout was reviewed at commit
  `9731ad3f29da96f588711a0d1eb08cf210721e16` and is pinned in
  `adapters/llama_cpp/upstream.json`; the verifier follows its `conversion/<model>.py` and
  `src/models/<model>.cpp` layout.
- The converter accepts only F16/BF16, checks GGUF magic, records exact commands/revisions/hashes,
  and never labels the model as Llama.
- The quantizer accepts only a high-precision GGUF source and Q4_K_M, validates optional calibration
  provenance/contamination fields, and always emits `publish_ready=false` until runtime and quality
  gates are supplied.
- This repository does not yet contain the required upstream llama.cpp C++ graph patch. There are no
  GGUF artifacts, Windows CUDA CLI/server parity results, or quantization regressions; MF-073/074
  remain In progress.

## Verification

- Focused M10/M11 plus CLI suite: **41 passed** in 98.36 seconds.
- Fast suite: **178 passed, 4 slow deselected** in 105.70 seconds.
- Complete suite: **182 passed** in 149.33 seconds.
- Ruff lint passed; Ruff format check passed for 128 Python files; Jira XML parsed successfully.
- The clean source-archive builder completed successfully; the final archive count and SHA-256 are
  reported alongside the generated artifact so the archive does not contain a self-referential hash.
- The sole warning is the existing expected CPU notice that eager FlexAttention is unfused. No CUDA,
  vLLM, llama.cpp, GGUF conversion, or quantization result is inferred from these CPU tests.

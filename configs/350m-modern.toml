preset = "modern"
vocab_size = 16384
max_seq_len = 2048
n_layers = 28
d_model = 1024
n_heads = 16
n_kv_heads = 4
d_ff = 2816
norm_eps = 1e-6
rope_theta = 10000.0
qk_norm = true
attention_pattern = "hybrid"
local_window = 512
global_position_encoding = "rope"
dropout = 0.0
tie_embeddings = true
attention_impl = "auto"
layer_norm_scaling = true
gated_attention = true
# MF-070 (2026-09-14): head_dim_override=96 (MF-108's 150M-validated default)
# real-tested at 350M's actual declared max_seq_len=2048 and rejected -- a
# real, confirmed VRAM-paging collapse (~70 tok/s, ~7.9GB/8GB, same signature
# as head_dim=128's own 150M collapse), not merely disclosed cost this time.
# head_dim derives to 64 (d_model/n_heads = 1024/16) without this override.
# See tasks/backlog.md's MF-070 entry for the real numbers.

